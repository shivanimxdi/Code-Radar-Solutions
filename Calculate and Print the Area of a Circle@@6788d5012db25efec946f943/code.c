#include <stdio.h>
#include <math.h>
int main() {
    float radius;
    double Area;
    printf("Area: ");
    scanf("%f",&radius);
    Area=(pi*radius*radius);
    printf("%f",Area);
    return 0;
}